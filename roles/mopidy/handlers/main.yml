- name: Restart pulseaudio
  ansible.builtin.systemd_service:
    name: pulseaudio
    state: restarted
    scope: user
  become: true
  become_user: "{{ user }}"

- name: Restart mopidy
  ansible.builtin.systemd_service:
    name: mopidy
    state: restarted
