---
- name: Configures all rpis
  hosts: kitchen_pi
  tasks:
    - name: Update all packages to their latest version # noqa: package-latest
      ansible.builtin.apt:
        name: "*"
        state: latest
